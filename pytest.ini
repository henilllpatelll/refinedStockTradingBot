[pytest]
filterwarnings =
    # alpaca-py uses websockets.legacy internally; not fixable in our code.
    # Fix: pip install "websockets>=12,<14" to stay below the deprecation boundary.
    ignore:websockets.legacy is deprecated:DeprecationWarning
