# Alpaca brokerage SDK
alpaca-py==0.43.4

# Data manipulation
pandas==2.2.3
numpy==2.1.2

# HTML scraping (Finviz universe sweep)
beautifulsoup4==4.12.3
requests==2.32.3

# High-performance binary serialization (inter-agent messaging)
msgpack==1.1.2

# Async HTTP client (Track-A REST snapshots)
aiohttp==3.13.5

# WebSocket client (Track-B tick feed)
# Pin below 14.0: alpaca-py uses websockets.legacy which was deprecated in 14.0
websockets>=12.0,<14.0

# Environment variable management
python-dotenv==1.0.1

# Wake-lock fallback (primary path uses ctypes — no dependency)
pyautogui==0.9.54

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
